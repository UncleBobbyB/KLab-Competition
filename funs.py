def process_review(raw_review):
    """Function to convert a raw review to a string of words
    
    Args:
        raw_review: single string

    Returns:
        single string, which is a processed review
    """
    # Convert to lower case
    review = raw_review.lower()

    return review
        
